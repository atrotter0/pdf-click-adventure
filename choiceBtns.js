//choice buttons

//choiceBtn1
var selected = currentSection.choices[0];
makeChoice(selected);

//choiceBtn2
var selected = currentSection.choices[1];
makeChoice(selected);

//choiceBtn3
var selected = currentSection.choices[2];
makeChoice(selected);

//choiceBtn4
var selected = currentSection.choices[3];
makeChoice(selected);